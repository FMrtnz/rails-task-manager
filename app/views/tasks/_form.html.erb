<div class="container text-center padding">
<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.check_box :done %>

  <%= f.submit %>
<% end %>
</div>
